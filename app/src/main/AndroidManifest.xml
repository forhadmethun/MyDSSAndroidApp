<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elitesbd.mydss">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".AboutActivity"
            android:label="@string/title_activity_maps"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MapsActivity3"
            android:label="@string/title_activity_maps3"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityCitizenCharter"
            android:label="@string/title_activity_citizen_charter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityHelpCenter"
            android:label="@string/title_activity_help_center"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityContact"
            android:label="@string/title_activity_contact"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityLifeEvents"
            android:label="@string/title_activity_life_events"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityPhotoGallery"
            android:label="@string/title_activity_photo_gallery"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityHeadQuarter"
            android:label="@string/title_activity_head_quarter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityDistrictOffice"
            android:label="@string/title_activity_district_office"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityGalleryWithViewPager"
            android:label="@string/title_activity_gallery_with_view_pager"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>